@startuml
skinparam packageBackgroundColor #2D2D44
skinparam packageBorderColor #7AA2F7
skinparam packageFontColor #7AA2F7
skinparam classBackgroundColor #363654
skinparam classBorderColor #BB9AF7
skinparam classHeaderBackgroundColor #414868
skinparam classFontColor #C0CAF5
skinparam arrowColor #7DCFFF
skinparam componentStyle uml2
skinparam linetype ortho
skinparam nodesep 50
skinparam ranksep 70

package "Componentes" {
    class AppComponent {
        +title: Signal<string>
    }

    class NavbarComponent {
        -searchTerm: Signal<string>
        -products: Signal<Product[]>
        -showResults: Signal<boolean>
        -filteredProducts: Computed
        -cartCount: Computed
        +onSearchInput()
        +onSearchFocus()
        +onSearchBlur()
        +selectProduct()
    }

    class CatalogoComponent {
        -products: Signal<Product[]>
        -filteredProducts: Computed
        -inStockCount: Computed
        +agregar()
    }

    class CarritoComponent {
        -carrito: Signal<Product[]>
        -total: Computed
        +quitar()
        +vaciar()
        +exportarXML()
    }

    class ProductCardComponent {
        +product: Product (Input)
        +add: EventEmitter (Output)
        +onAdd()
    }
}

package "Servicios" {
    class ProductsService {
        -productosMock: Product[]
        +getAll(): Observable
        +parseXml()
    }

    class CarritoService {
        -productosSignal: Signal
        +agregar()
        +quitar()
        +vaciar()
        +total()
        +exportarXML()
    }

    class SearchService {
        -searchTermSignal: Signal
        +setSearchTerm()
        +clearSearch()
    }
}

package "Modelos" {
    interface Product {
        +id: number
        +name: string
        +price: number
        +description?: string
        +category: string
        +imageUrl: string
        +inStock: boolean
    }
}

AppComponent --> NavbarComponent
AppComponent --> CatalogoComponent
AppComponent --> CarritoComponent

CatalogoComponent --> ProductCardComponent
ProductCardComponent --> Product

CatalogoComponent --> ProductsService
CatalogoComponent --> CarritoService
CatalogoComponent --> SearchService

NavbarComponent --> ProductsService
NavbarComponent --> CarritoService
NavbarComponent --> SearchService

CarritoComponent --> CarritoService
CarritoComponent --> Product
@enduml
